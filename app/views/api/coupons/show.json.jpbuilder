json.coupon do
  json.code @coupon.code
  json.expires_at @coupon.expires_at
  json.status @coupon.status
  json.amount do
    json.amount @coupon.asset_amount
    json.symbol @coupon.asset_symbol
    json.precision @coupon.asset_precision
    json.asset_id @coupon.asset_blockchainid
  end
end
